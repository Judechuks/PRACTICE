// Higher Order Function is a function that does at least one of the following:
// i. Takes one or more functions as an argument (parameter)
// ii. Returns a function as its result 
// Examples :
// forEach()
// filter()
// map()
// reduce()

// See the array and string method treated before
array.forEach(element => {
  console.log(element);
});
