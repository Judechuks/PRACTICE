// Keydown = an event, when a user press down a key on the keyboard.
// Keyup = an event, when a user releases a key on the keyboard, after a keydown event.
window.addEventListener("keydown", event => console.log(event.key));
